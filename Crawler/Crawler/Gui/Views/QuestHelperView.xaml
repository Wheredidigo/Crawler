<UserControl x:Class="Crawler.Gui.Views.QuestHelperView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:settings="clr-namespace:Crawler.Settings"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <settings:CrawlerSettings x:Key="CrawlerSettings" d:IsDataSource="True"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <CheckBox Content="Skip Cutscenes" Foreground="Red" IsChecked="{Binding Instance.SkipCutscenes, Mode=TwoWay, Source={StaticResource CrawlerSettings}, UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox Content="Skip Dialog" Foreground="Red" IsChecked="{Binding Instance.SkipDialog, Mode=TwoWay, Source={StaticResource CrawlerSettings}, UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox Content="Accept Quests" Foreground="Red" IsChecked="{Binding Instance.AcceptQuests, Mode=TwoWay, Source={StaticResource CrawlerSettings}, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
    </Grid>
</UserControl>
