<UserControl x:Class="Crawler.Gui.Views.TargetingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:settings="clr-namespace:Crawler.Settings"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <settings:CrawlerSettings x:Key="CrawlerSettings" d:IsDataSource="True"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <CheckBox Content="Use Auto Targeting" Foreground="Red" IsChecked="{Binding Instance.UseAutoTargeting, Mode=TwoWay, Source={StaticResource CrawlerSettings}, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
